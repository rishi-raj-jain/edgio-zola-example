{
  "name": "zola",
  "scripts": {
    "edgio:dev": "edgio dev",
    "edgio:build": "zola build && edgio build",
    "edgio:deploy": "zola build && edgio deploy",
    "format": "prettier --write '**/*' --ignore-unknown"
  },
  "devDependencies": {
    "@edgio/cli": "latest",
    "@edgio/core": "latest",
    "prettier": "latest"
  }
}
